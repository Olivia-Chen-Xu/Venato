{
  "functions": [
    {
      "predeploy": ["npm --prefix \"$RESOURCE_DIR\" run build"],
      "source": "src/backend",
      "codebase": "default",
      "ignore": [
        "node_modules",
        ".git",
        "firebase-debug.log",
        "firebase-debug.*.log"
      ]
    }
  ]
}
